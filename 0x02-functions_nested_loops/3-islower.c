#include "main.h"

/**
 * _islower - Check for lowercase charters
 * * @c: The character to be checked
 * Return: 1 for lowercase characters or 0 for anything else
 */

int  _islower(int c)

{
	If (c >= 97 && c <= 122)
	{

	Return (1);
	}
	Return (0);
}
